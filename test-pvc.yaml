apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-data
  namespace: ns-devith
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 14Gi